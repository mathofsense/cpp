INPUT a,b,c
IF a + b > c THEN 
  IF a + c > b THEN
    IF b + c > a THEN
      OUTPUT "Can make a triangle"
    ELSE
      OUTPUT "Can't make a triangle"
    ENDIF
  ENDIF
  ??? // Complete the else's
ENDIF
